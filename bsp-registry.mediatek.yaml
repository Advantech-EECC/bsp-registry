# =========================================================================
# MediaTek registry fragment
#
# Contains the registry entries that are exclusive to the MediaTek vendor:
# the MediaTek vendor definition, the MediaTek reference devices and
# their BSP presets.
#
# This file is not a stand-alone registry: it is pulled in by the top-level
# `bsp-registry.yml` through its `include:` directive and merged into it
# (lists are concatenated, so the entries below extend the corresponding
# sections of the main registry).
# =========================================================================

registry:

  # =====================================
  # Vendors
  # =====================================
  vendors:

    - slug: mediatek
      name: "Mediatek"
      description: "Mediatek, a global fabless semiconductor company providing chips for various applications."
      website: "https://www.mediatek.com/"
      includes:
        - vendors/mediatek/mediatek-common.yml

  # =====================================
  # Devices
  # =====================================
  devices:

    # --- Mediatek Devices ---

    - slug: genio-350-evk
      description: "Mediatek genio-350-evk"
      vendor: mediatek
      soc_vendor: mediatek
      architecture: arm64
      includes:
        - vendors/mediatek/machine/genio-350-evk.yml

    - slug: genio-510-evk
      description: "Mediatek genio-510-evk"
      vendor: mediatek
      soc_vendor: mediatek
      architecture: arm64
      includes:
        - vendors/mediatek/machine/genio-510-evk.yml

    - slug: genio-700-evk
      description: "Mediatek genio-700-evk"
      vendor: mediatek
      soc_vendor: mediatek
      architecture: arm64
      includes:
        - vendors/mediatek/machine/genio-700-evk.yml

    - slug: genio-1200-evk
      description: "Mediatek genio-1200-evk"
      vendor: mediatek
      soc_vendor: mediatek
      architecture: arm64
      includes:
        - vendors/mediatek/machine/genio-1200-evk.yml

  # =====================================
  # BSP Presets (named shortcuts)
  # =====================================
  bsp:

    # --- Mediatek Genio 350 EVK ---

    - name: mediatek-genio-350-evk
      description: "Mediatek Genio 350 EVK"
      device: genio-350-evk
      releases: [scarthgap]
      features: [systemd, security, virtualization, ipv6]
      build:
        path: build/mediatek-genio-350-evk

    # --- Mediatek Genio 510 EVK ---

    - name: mediatek-genio-510-evk
      description: "Mediatek Genio 510 EVK"
      device: genio-510-evk
      releases: [scarthgap]
      features: [systemd, security, virtualization, ipv6]
      build:
        path: build/mediatek-genio-510-evk

    # --- Mediatek Genio 700 EVK ---

    - name: mediatek-genio-700-evk
      description: "Mediatek Genio 700 EVK"
      device: genio-700-evk
      releases: [scarthgap]
      features: [systemd, security, virtualization, ipv6]
      build:
        path: build/mediatek-genio-700-evk

    # --- Mediatek Genio 1200 EVK ---    

    - name: mediatek-genio-1200-evk
      description: "Mediatek Genio 1200 EVK"
      device: genio-1200-evk
      releases: [scarthgap]
      features: [systemd, security, virtualization, ipv6]
      build:
        path: build/mediatek-genio-1200-evk
